<% provide(:title, t('admin.lands.title')) %>

<div class="right_col" role="main">
  <div class="">

    <%= render :partial => 'admin/shared/page_title', :locals => {title: t('admin.lands.title'), description: t('admin.lands.description')} %>
    <div class="clearfix"></div>
    <div class="row">
      <div class="clearfix"></div>
      <div class="col-md-12 col-sm-12 col-xs-12">

        <div class="x_panel">
          <%= render :partial => 'admin/shared/table_title', :locals => {title: t('admin.elements.search.button'), description: ''} %>
          <div class="x_content">
            <%= render 'search_form' %>
          </div>
        </div>

        <%= render 'layouts/admin_menus/lands_sub_tab' %>

        <div class="x_panel">
          <%= render :partial => 'admin/shared/table_title', :locals => {title: t('admin.lands.title'), description: t('admin.lands.description')} %>
          <div class="x_content">

            <%= render 'pagination' %>
            <%= render :partial => 'table_contents', :locals => {lands: @lands} %>
            <%= render 'pagination' %>

            <br>
            <%= set_button_new(t('admin.lands.buttons.add'), new_admin_land_path) %>
            <%= set_button_csv(t('admin.lands.buttons.csv_export'), admin_lands_path(format: 'csv')) %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>