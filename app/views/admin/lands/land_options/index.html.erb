<% provide(:title, t('admin.land_options.title')) %>

<div class="right_col" role="main">
  <div class="">

    <%= render :partial => 'admin/shared/page_title', :locals => {title: t('admin.land_options.title'), description: t('admin.land_options.description')} %>

    <div class="clearfix"></div>
    <div class="row">
      <div class="clearfix"></div>
      <div class="col-md-12 col-sm-12 col-xs-12">
        <%= render 'layouts/admin_menus/lands_sub_tab' %>

        <div class="x_panel">
          <%= render :partial => 'admin/shared/table_title', :locals => {title: t('admin.land_options.title'), description: t('admin.land_options.description')} %>
          <div class="x_content">

            <%= render 'pagination' %>
            <%= render :partial => 'table_contents', :locals => {land_options: @land_options} %>
            <%= render 'pagination' %>

            <br>
            <%= set_button_new(t('admin.land_options.buttons.add'), new_admin_land_option_path) %>
            <%= set_button_csv(t('admin.land_options.buttons.csv_export'), admin_land_options_path(format: 'csv')) %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>