<div class="table-responsive">
  <table class="table table-striped jambo_table bulk_action">
    <thead>
    <tr class="headings">
      <th>
        <input type="checkbox" id="check-all" class="flat">
      </th>
      <th class="column-title"><%= t('admin.lands.items.name') %></th>
      <th class="column-title"><%= t('admin.lands.items.created_at') %></th>
      <th class="column-title"><%= t('admin.lands.items.updated_at') %></th>
      <th class="column-title no-link last" colspan="3"><span class="nobr"><%= t('admin.lands.items.controls') %></span></th>
      <th class="bulk-actions" colspan="12">
        <a class="antoo" style="color:#fff; font-weight:500;"><%= t('admin.elements.buttons.bulc_action') %> ( <span class="action-cnt"> </span> ) <i class="fa fa-chevron-down"></i></a>
      </th>
    </tr>
    </thead>

    <tbody>
    <% @land_options.each do |land_option| %>
        <tr class="even pointer">
          <td class="a-center">
            <input type="checkbox" class="flat" name="table_records">
          </td>
          <td> <%= land_option.name %></td>
          <td> <%= land_option.formatted_created_date %></td>
          <td> <%= land_option.formatted_updated_date %></td>
          <td> <%= set_button_show('', [:admin, land_option]) %></td>
          <td> <%= set_button_edit('', edit_admin_land_option_path(land_option)) %></td>
          <td> <%= set_button_delete('', [:admin, land_option]) %></td>
        </tr>
    <% end %>
    </tbody>
  </table>
</div>