<div class="table-responsive">
  <table class="table table-striped jambo_table bulk_action">
    <thead>
    <tr class="headings">
      <th>
        <input type="checkbox" id="check-all" class="flat">
      </th>
      <th class="column-title"><%= t('admin.condos.items.name') %></th>
      <th class="column-title"><%= t('admin.condos.items.created_at') %></th>
      <th class="column-title"><%= t('admin.condos.items.updated_at') %></th>
      <th class="column-title no-link last" colspan="3"><span class="nobr"><%= t('admin.condos.items.controls') %></span></th>
      <th class="bulk-actions" colspan="12">
        <a class="antoo" style="color:#fff; font-weight:500;"><%= t('admin.elements.buttons.bulc_action') %> ( <span class="action-cnt"> </span> ) <i class="fa fa-chevron-down"></i></a>
      </th>
    </tr>
    </thead>

    <tbody>
    <% @condo_options.each do |condo_option| %>
        <tr class="even pointer">
          <td class="a-center">
            <input type="checkbox" class="flat" name="table_records">
          </td>
          <td> <%= condo_option.name %></td>
          <td> <%= condo_option.formatted_created_date %></td>
          <td> <%= condo_option.formatted_updated_date %></td>
          <td> <%= set_button_show('', [:admin, condo_option]) %></td>
          <td> <%= set_button_edit('', edit_admin_condo_option_path(condo_option)) %></td>
          <td> <%= set_button_delete('', [:admin, condo_option]) %></td>
        </tr>
    <% end %>
    </tbody>
  </table>
</div>