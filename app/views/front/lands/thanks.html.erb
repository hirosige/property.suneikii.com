<div class="container">
  <div class="inquiry-thanks">
    <div class="page-header">
      <h3><%= t('front.lands.thanks.title') %></h3>
    </div>

    <p><%= t('front.lands.thanks.description') %></p>
    <p><%= t('front.lands.thanks.greeting') %></p>
  </div>
</div>