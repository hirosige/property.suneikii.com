<header>
  <div class="list-header">
    <div class="list-header--left">
      <div class="logo"><%= Settings.site[:name] %></div>
      <div class="description"><a href="#">物件数No.1の不動産・住宅情報サイト <%= Settings.site[:name] %></a></div>
    </div>

    <div class="list-header--right">
      <% if user_signed_in? %>
          <div class="dropdown">
            <button class="btn btn-sm btn-primary dropdown-toggle list-header_info--signed-in" type="button" id="dropdownMenu1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
              <%= image_tag current_user.gravatar_url, :class => 'control-box__gravatar' %> <%= current_user.name %>
              <% if Rails.env == 'development' %>
                  <span class="label label-success">:<%= current_user.role %></span>
              <% end %>
              <span class="caret"></span>
            </button>
            <ul class="dropdown-menu" aria-labelledby="dropdownMenu1">
              <li><a href="#">Action</a></li>
              <li><a href="#">Another action</a></li>
              <li><a href="#">Something else here</a></li>
              <li role="separator" class="divider"></li>
              <li><a href="#">Separated link</a></li>
            </ul>
          </div>
      <% end %>

      <%= cell("front/sessions/sign_in", current_user).(:sign_in_btn) %>
    </div>
  </div>

  <div class="control-box">
    <div class="control-box--left">
      <%= image_tag("snk_logo.jpg", :style => 'width: 100px;', :alt => 'logo') %>
      <button class="control-box__btn" onclick="location.href='<%= apartments_path %>'">
        <i class="fa fa-building"></i>
        アパート
      </button>
      <button class="control-box__btn" disabled>
        <i class="fa fa-plus-square-o"></i>
        土地
      </button>
      <button class="control-box__btn" disabled>
        <i class="fa fa-building-o"></i>
        コンドミニアム
      </button>
    </div>

    <div class="control-box__login-info">
      <p><i class="fa fa-search control-box__icon"></i></p>
      <p class="control-box__counter">
        <% if session[:apartments_session_list].nil? %>
            <%= 0 %>
        <% else %>
            <%= session[:apartments_session_list].size unless session[:apartments_session_list].nil? %>
        <% end %>

      </p>
      <p>最近見た物件</p>
    </div>

    <div class="control-box__login-info">
      <p><i class="fa fa-save control-box__icon"></i></p>
      <p class="control-box__counter">
        <% if session[:apartments_session_list].nil? %>
            <%= 0 %>
        <% else %>
            <%= session[:apartments_session_list].size unless session[:apartments_session_list].nil? %>
        <% end %>
      </p>
      <p>お気に入り</p>
    </div>

  </div>
</header>

<% module_name = params[:controller].split('/')[0] %>
<% unless params[:action] == 'render_404' or module_name == 'admin' %>
  <div class="header-info">
    <div class="container">
      <% no_info_controllers = Array.new %>
      <% no_info_controllers.push('registrations', 'sessions', 'confirmations', 'passwords', 'omniauth_callbacks') %>
      <% unless no_info_controllers.include?(controller.controller_name) %>
        <% if params[:province].nil? %>
            <%= params[:controller].split('/')[1].capitalize %> を検索
        <% else %>
            <%= params[:province].capitalize unless params[:province].nil? %>の<%= params[:controller].split('/')[1].capitalize %>
        <% end %>

        物件一覧ページ。面積（坪数）や価格（坪単価）で絞り込み、簡単に比較・資料請求。
      <% end %>
    </div>
  </div>
<% end %>

<div class="breadcrumb">
  <div class="container">
    <%= render_breadcrumbs separator: ' ▶ ' %>
  </div>
</div>

<div id="page-top">
  <button class="move-top-btn">
    <i class="fa fa-chevron-up"></i>
  </button>
</div>